/**
 * Tranform values to currency values with "," " " and "." separators
 * @param {number} values Value to transform
 * @param {string} currencyCode target currency code 
 */

export const formatPrices = ( values, currencyCode ) =>
{

};
